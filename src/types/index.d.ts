export interface CarsouelItem {
  index: number,
  url: string
}

export interface article {
  id: number,
  backgroundImage: string,
  url: string,
  title: string,
  summary: string,
  publishDate: string,
  category: string,
  tags: string[]
}